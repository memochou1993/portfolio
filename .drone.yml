---
kind: pipeline
name: default

steps:
- name: build
  image: laradock/workspace:2.6.1-7.1
  commands:
  - php -v
  - composer -V
  - cp .env.example .env
  - composer install --prefer-dist
  - php artisan key:generate
  - php artisan migrate
  - ./vendor/bin/phpunit
...
